<IfModule mod_rewrite.c>
	RewriteEngine On
	RewriteCond %{HTTPS} off
	RewriteCond %{HTTP_HOST} ^bills.partnersbuddy\.in [NC]
	RewriteCond %{SERVER_PORT} 80
	RewriteRule ^(.*)$ https://bills.partnersbuddy.in/$1 [R,L]
</IfModule>